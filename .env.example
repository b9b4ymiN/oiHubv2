# Binance API Configuration
# Default: Direct connection to Binance (may face geo-restrictions on Vercel)
NEXT_PUBLIC_BINANCE_API_URL=https://fapi.binance.com
NEXT_PUBLIC_BINANCE_WS_URL=wss://fstream.binance.com

# IMPORTANT: If you get "451 - Service unavailable from restricted location" error
# Deploy the included cloudflare-worker.js and uncomment the line below:
# NEXT_PUBLIC_BINANCE_API_URL=https://binance-proxy.your-domain.workers.dev

# Alternative: Use Binance US (if in United States)
# NEXT_PUBLIC_BINANCE_API_URL=https://fapi.binance.us

# Optional: For authenticated endpoints (rate limit increase)
# BINANCE_API_KEY=your_api_key_here
# BINANCE_API_SECRET=your_api_secret_here

# Redis for caching (optional)
# REDIS_URL=redis://localhost:6379

# Rate limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# AI Chat Assistant API
CHAT_API_URL=http://openAI/chat
